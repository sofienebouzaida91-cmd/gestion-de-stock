# Base URL of the Cagette API (see ../server).
# localhost works for the iOS Simulator and web; on a physical device or the
# Android emulator, use your machine's LAN IP instead, e.g. http://192.168.1.23:4000
EXPO_PUBLIC_API_URL=http://localhost:4000
